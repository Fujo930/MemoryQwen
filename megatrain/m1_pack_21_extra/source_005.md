# Extra 21 Doc 5

##1
MemoryQwen v0.1 训练数据1。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##2
MemoryQwen v0.1 训练数据2。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##3
MemoryQwen v0.1 训练数据3。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##4
MemoryQwen v0.1 训练数据4。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##5
MemoryQwen v0.1 训练数据5。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##6
MemoryQwen v0.1 训练数据6。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##7
MemoryQwen v0.1 训练数据7。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##8
MemoryQwen v0.1 训练数据8。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##9
MemoryQwen v0.1 训练数据9。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##10
MemoryQwen v0.1 训练数据10。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##11
MemoryQwen v0.1 训练数据11。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##12
MemoryQwen v0.1 训练数据12。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##13
MemoryQwen v0.1 训练数据13。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##14
MemoryQwen v0.1 训练数据14。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##15
MemoryQwen v0.1 训练数据15。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##16
MemoryQwen v0.1 训练数据16。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##17
MemoryQwen v0.1 训练数据17。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##18
MemoryQwen v0.1 训练数据18。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##19
MemoryQwen v0.1 训练数据19。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##20
MemoryQwen v0.1 训练数据20。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##21
MemoryQwen v0.1 训练数据21。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##22
MemoryQwen v0.1 训练数据22。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##23
MemoryQwen v0.1 训练数据23。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##24
MemoryQwen v0.1 训练数据24。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##25
MemoryQwen v0.1 训练数据25。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##26
MemoryQwen v0.1 训练数据26。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##27
MemoryQwen v0.1 训练数据27。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##28
MemoryQwen v0.1 训练数据28。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##29
MemoryQwen v0.1 训练数据29。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
##30
MemoryQwen v0.1 训练数据30。本地AI agent系统。7B常驻3B smoke。CLI入口。支持txt和md文件导入。BM25关键词检索。GPU Guardian检测。Task Runtime任务管理。Eval Runner评测系统。不支持WebUI/PDF/embedding/daemon/crawler/LoRA。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。
## 标签
v0.1,megatrain,m1
