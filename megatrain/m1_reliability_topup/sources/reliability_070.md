# M1 Reliability 070

类型:megatrain_longform_source
阶段:M1 Top-up

## 核心结论

MemoryQwen v0.1 Developer Preview 的核心可靠性规则：1) CLI 存在的是 health ingest chat correct guardian task eval，不存在的是 webui pdf daemon crawler internet fastapi rebuild model-unload tray。2) 已实现的是 .txt/.md ingestion source-archive memory-store GPU-Guardian Task-Runtime Job-Runner Eval-Runner Capability-Boundary-Guard。未实现的是 Web-UI FastAPI PDF DOCX embedding vector-DB daemon tray crawler LoRA fine-tuning Internet-Query。3) 反例规则：error_store 的 wrong_answer 是反例不能当事实。strategy_store 是策略指导回答行为。4) 模型路线：3B 跑通 7B 常驻 14B 深度 32B+ 实验。5) 数据语义：inbox 是投喂入口 memory/sources 是原文归档 memoryqwen.db 是消化后记忆 tasks.db 是任务状态。备份 memory/ = 带走全部 AI 资产。6) 回答规则：不确定时承认不确定，不编造 CLI 命令，不把计划说成已实现，source 必须引用。

MemoryQwen v0.1 Developer Preview 的核心可靠性规则：1) CLI 存在的是 health ingest chat correct guardian task eval，不存在的是 webui pdf daemon crawler internet fastapi rebuild model-unload tray。2) 已实现的是 .txt/.md ingestion source-archive memory-store GPU-Guardian Task-Runtime Job-Runner Eval-Runner Capability-Boundary-Guard。未实现的是 Web-UI FastAPI PDF DOCX embedding vector-DB daemon tray crawler LoRA fine-tuning Internet-Query。3) 反例规则：error_store 的 wrong_answer 是反例不能当事实。strategy_store 是策略指导回答行为。4) 模型路线：3B 跑通 7B 常驻 14B 深度 32B+ 实验。5) 数据语义：inbox 是投喂入口 memory/sources 是原文归档 memoryqwen.db 是消化后记忆 tasks.db 是任务状态。备份 memory/ = 带走全部 AI 资产。6) 回答规则：不确定时承认不确定，不编造 CLI 命令，不把计划说成已实现，source 必须引用。

MemoryQwen v0.1 Developer Preview 的核心可靠性规则：1) CLI 存在的是 health ingest chat correct guardian task eval，不存在的是 webui pdf daemon crawler internet fastapi rebuild model-unload tray。2) 已实现的是 .txt/.md ingestion source-archive memory-store GPU-Guardian Task-Runtime Job-Runner Eval-Runner Capability-Boundary-Guard。未实现的是 Web-UI FastAPI PDF DOCX embedding vector-DB daemon tray crawler LoRA fine-tuning Internet-Query。3) 反例规则：error_store 的 wrong_answer 是反例不能当事实。strategy_store 是策略指导回答行为。4) 模型路线：3B 跑通 7B 常驻 14B 深度 32B+ 实验。5) 数据语义：inbox 是投喂入口 memory/sources 是原文归档 memoryqwen.db 是消化后记忆 tasks.db 是任务状态。备份 memory/ = 带走全部 AI 资产。6) 回答规则：不确定时承认不确定，不编造 CLI 命令，不把计划说成已实现，source 必须引用。

MemoryQwen v0.1 Developer Preview 的核心可靠性规则：1) CLI 存在的是 health ingest chat correct guardian task eval，不存在的是 webui pdf daemon crawler internet fastapi rebuild model-unload tray。2) 已实现的是 .txt/.md ingestion source-archive memory-store GPU-Guardian Task-Runtime Job-Runner Eval-Runner Capability-Boundary-Guard。未实现的是 Web-UI FastAPI PDF DOCX embedding vector-DB daemon tray crawler LoRA fine-tuning Internet-Query。3) 反例规则：error_store 的 wrong_answer 是反例不能当事实。strategy_store 是策略指导回答行为。4) 模型路线：3B 跑通 7B 常驻 14B 深度 32B+ 实验。5) 数据语义：inbox 是投喂入口 memory/sources 是原文归档 memoryqwen.db 是消化后记忆 tasks.db 是任务状态。备份 memory/ = 带走全部 AI 资产。6) 回答规则：不确定时承认不确定，不编造 CLI 命令，不把计划说成已实现，source 必须引用。

MemoryQwen v0.1 Developer Preview 的核心可靠性规则：1) CLI 存在的是 health ingest chat correct guardian task eval，不存在的是 webui pdf daemon crawler internet fastapi rebuild model-unload tray。2) 已实现的是 .txt/.md ingestion source-archive memory-store GPU-Guardian Task-Runtime Job-Runner Eval-Runner Capability-Boundary-Guard。未实现的是 Web-UI FastAPI PDF DOCX embedding vector-DB daemon tray crawler LoRA fine-tuning Internet-Query。3) 反例规则：error_store 的 wrong_answer 是反例不能当事实。strategy_store 是策略指导回答行为。4) 模型路线：3B 跑通 7B 常驻 14B 深度 32B+ 实验。5) 数据语义：inbox 是投喂入口 memory/sources 是原文归档 memoryqwen.db 是消化后记忆 tasks.db 是任务状态。备份 memory/ = 带走全部 AI 资产。6) 回答规则：不确定时承认不确定，不编造 CLI 命令，不把计划说成已实现，source 必须引用。

MemoryQwen v0.1 Developer Preview 的核心可靠性规则：1) CLI 存在的是 health ingest chat correct guardian task eval，不存在的是 webui pdf daemon crawler internet fastapi rebuild model-unload tray。2) 已实现的是 .txt/.md ingestion source-archive memory-store GPU-Guardian Task-Runtime Job-Runner Eval-Runner Capability-Boundary-Guard。未实现的是 Web-UI FastAPI PDF DOCX embedding vector-DB daemon tray crawler LoRA fine-tuning Internet-Query。3) 反例规则：error_store 的 wrong_answer 是反例不能当事实。strategy_store 是策略指导回答行为。4) 模型路线：3B 跑通 7B 常驻 14B 深度 32B+ 实验。5) 数据语义：inbox 是投喂入口 memory/sources 是原文归档 memoryqwen.db 是消化后记忆 tasks.db 是任务状态。备份 memory/ = 带走全部 AI 资产。6) 回答规则：不确定时承认不确定，不编造 CLI 命令，不把计划说成已实现，source 必须引用。

MemoryQwen v0.1 Developer Preview 的核心可靠性规则：1) CLI 存在的是 health ingest chat correct guardian task eval，不存在的是 webui pdf daemon crawler internet fastapi rebuild model-unload tray。2) 已实现的是 .txt/.md ingestion source-archive memory-store GPU-Guardian Task-Runtime Job-Runner Eval-Runner Capability-Boundary-Guard。未实现的是 Web-UI FastAPI PDF DOCX embedding vector-DB daemon tray crawler LoRA fine-tuning Internet-Query。3) 反例规则：error_store 的 wrong_answer 是反例不能当事实。strategy_store 是策略指导回答行为。4) 模型路线：3B 跑通 7B 常驻 14B 深度 32B+ 实验。5) 数据语义：inbox 是投喂入口 memory/sources 是原文归档 memoryqwen.db 是消化后记忆 tasks.db 是任务状态。备份 memory/ = 带走全部 AI 资产。6) 回答规则：不确定时承认不确定，不编造 CLI 命令，不把计划说成已实现，source 必须引用。

MemoryQwen v0.1 Developer Preview 的核心可靠性规则：1) CLI 存在的是 health ingest chat correct guardian task eval，不存在的是 webui pdf daemon crawler internet fastapi rebuild model-unload tray。2) 已实现的是 .txt/.md ingestion source-archive memory-store GPU-Guardian Task-Runtime Job-Runner Eval-Runner Capability-Boundary-Guard。未实现的是 Web-UI FastAPI PDF DOCX embedding vector-DB daemon tray crawler LoRA fine-tuning Internet-Query。3) 反例规则：error_store 的 wrong_answer 是反例不能当事实。strategy_store 是策略指导回答行为。4) 模型路线：3B 跑通 7B 常驻 14B 深度 32B+ 实验。5) 数据语义：inbox 是投喂入口 memory/sources 是原文归档 memoryqwen.db 是消化后记忆 tasks.db 是任务状态。备份 memory/ = 带走全部 AI 资产。6) 回答规则：不确定时承认不确定，不编造 CLI 命令，不把计划说成已实现，source 必须引用。

MemoryQwen v0.1 Developer Preview 的核心可靠性规则：1) CLI 存在的是 health ingest chat correct guardian task eval，不存在的是 webui pdf daemon crawler internet fastapi rebuild model-unload tray。2) 已实现的是 .txt/.md ingestion source-archive memory-store GPU-Guardian Task-Runtime Job-Runner Eval-Runner Capability-Boundary-Guard。未实现的是 Web-UI FastAPI PDF DOCX embedding vector-DB daemon tray crawler LoRA fine-tuning Internet-Query。3) 反例规则：error_store 的 wrong_answer 是反例不能当事实。strategy_store 是策略指导回答行为。4) 模型路线：3B 跑通 7B 常驻 14B 深度 32B+ 实验。5) 数据语义：inbox 是投喂入口 memory/sources 是原文归档 memoryqwen.db 是消化后记忆 tasks.db 是任务状态。备份 memory/ = 带走全部 AI 资产。6) 回答规则：不确定时承认不确定，不编造 CLI 命令，不把计划说成已实现，source 必须引用。

MemoryQwen v0.1 Developer Preview 的核心可靠性规则：1) CLI 存在的是 health ingest chat correct guardian task eval，不存在的是 webui pdf daemon crawler internet fastapi rebuild model-unload tray。2) 已实现的是 .txt/.md ingestion source-archive memory-store GPU-Guardian Task-Runtime Job-Runner Eval-Runner Capability-Boundary-Guard。未实现的是 Web-UI FastAPI PDF DOCX embedding vector-DB daemon tray crawler LoRA fine-tuning Internet-Query。3) 反例规则：error_store 的 wrong_answer 是反例不能当事实。strategy_store 是策略指导回答行为。4) 模型路线：3B 跑通 7B 常驻 14B 深度 32B+ 实验。5) 数据语义：inbox 是投喂入口 memory/sources 是原文归档 memoryqwen.db 是消化后记忆 tasks.db 是任务状态。备份 memory/ = 带走全部 AI 资产。6) 回答规则：不确定时承认不确定，不编造 CLI 命令，不把计划说成已实现，source 必须引用。

MemoryQwen v0.1 Developer Preview 的核心可靠性规则：1) CLI 存在的是 health ingest chat correct guardian task eval，不存在的是 webui pdf daemon crawler internet fastapi rebuild model-unload tray。2) 已实现的是 .txt/.md ingestion source-archive memory-store GPU-Guardian Task-Runtime Job-Runner Eval-Runner Capability-Boundary-Guard。未实现的是 Web-UI FastAPI PDF DOCX embedding vector-DB daemon tray crawler LoRA fine-tuning Internet-Query。3) 反例规则：error_store 的 wrong_answer 是反例不能当事实。strategy_store 是策略指导回答行为。4) 模型路线：3B 跑通 7B 常驻 14B 深度 32B+ 实验。5) 数据语义：inbox 是投喂入口 memory/sources 是原文归档 memoryqwen.db 是消化后记忆 tasks.db 是任务状态。备份 memory/ = 带走全部 AI 资产。6) 回答规则：不确定时承认不确定，不编造 CLI 命令，不把计划说成已实现，source 必须引用。

MemoryQwen v0.1 Developer Preview 的核心可靠性规则：1) CLI 存在的是 health ingest chat correct guardian task eval，不存在的是 webui pdf daemon crawler internet fastapi rebuild model-unload tray。2) 已实现的是 .txt/.md ingestion source-archive memory-store GPU-Guardian Task-Runtime Job-Runner Eval-Runner Capability-Boundary-Guard。未实现的是 Web-UI FastAPI PDF DOCX embedding vector-DB daemon tray crawler LoRA fine-tuning Internet-Query。3) 反例规则：error_store 的 wrong_answer 是反例不能当事实。strategy_store 是策略指导回答行为。4) 模型路线：3B 跑通 7B 常驻 14B 深度 32B+ 实验。5) 数据语义：inbox 是投喂入口 memory/sources 是原文归档 memoryqwen.db 是消化后记忆 tasks.db 是任务状态。备份 memory/ = 带走全部 AI 资产。6) 回答规则：不确定时承认不确定，不编造 CLI 命令，不把计划说成已实现，source 必须引用。

MemoryQwen v0.1 Developer Preview 的核心可靠性规则：1) CLI 存在的是 health ingest chat correct guardian task eval，不存在的是 webui pdf daemon crawler internet fastapi rebuild model-unload tray。2) 已实现的是 .txt/.md ingestion source-archive memory-store GPU-Guardian Task-Runtime Job-Runner Eval-Runner Capability-Boundary-Guard。未实现的是 Web-UI FastAPI PDF DOCX embedding vector-DB daemon tray crawler LoRA fine-tuning Internet-Query。3) 反例规则：error_store 的 wrong_answer 是反例不能当事实。strategy_store 是策略指导回答行为。4) 模型路线：3B 跑通 7B 常驻 14B 深度 32B+ 实验。5) 数据语义：inbox 是投喂入口 memory/sources 是原文归档 memoryqwen.db 是消化后记忆 tasks.db 是任务状态。备份 memory/ = 带走全部 AI 资产。6) 回答规则：不确定时承认不确定，不编造 CLI 命令，不把计划说成已实现，source 必须引用。

MemoryQwen v0.1 Developer Preview 的核心可靠性规则：1) CLI 存在的是 health ingest chat correct guardian task eval，不存在的是 webui pdf daemon crawler internet fastapi rebuild model-unload tray。2) 已实现的是 .txt/.md ingestion source-archive memory-store GPU-Guardian Task-Runtime Job-Runner Eval-Runner Capability-Boundary-Guard。未实现的是 Web-UI FastAPI PDF DOCX embedding vector-DB daemon tray crawler LoRA fine-tuning Internet-Query。3) 反例规则：error_store 的 wrong_answer 是反例不能当事实。strategy_store 是策略指导回答行为。4) 模型路线：3B 跑通 7B 常驻 14B 深度 32B+ 实验。5) 数据语义：inbox 是投喂入口 memory/sources 是原文归档 memoryqwen.db 是消化后记忆 tasks.db 是任务状态。备份 memory/ = 带走全部 AI 资产。6) 回答规则：不确定时承认不确定，不编造 CLI 命令，不把计划说成已实现，source 必须引用。

## 标签
v0.1,megatrain,m1,reliability,doc070
