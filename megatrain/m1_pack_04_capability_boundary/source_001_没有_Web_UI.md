# 没有 Web UI

类型:longform_training_source
适用版本:MemoryQwen v0.1.0-dev
更新时间:2026-06-27
是否可公开:yes

## 核心结论

关于没有 Web UI的详细知识文档。用于MemoryQwen MegaTrain M1训练。

## 详细内容

### 部分1

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分2

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分3

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分4

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分5

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分6

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分7

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分8

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分9

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分10

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分11

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分12

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分13

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分14

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分15

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分16

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分17

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分18

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分19

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分20

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分21

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分22

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分23

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分24

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分25

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分26

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分27

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分28

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分29

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分30

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分31

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分32

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分33

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分34

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分35

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分36

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分37

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分38

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分39

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分40

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分41

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分42

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分43

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分44

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分45

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分46

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分47

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分48

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分49

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

### 部分50

MemoryQwen v0.1 的相关知识：这是一个本地AI agent系统。核心设计是模型不负责记忆，MemoryBus管理所有长期状态。已实现功能包括CLI操作、.txt/.md文件导入、BM25关键词检索、GPU Guardian检测、Task Runtime任务管理。未实现功能包括WebUI、PDF/DOCX ingestion、embedding/向量搜索、daemon/tray、crawler、LoRA/模型微调。系统运行在Windows11上，使用qwen2.5:7b作为默认推荐模型，qwen2.5-coder:3b用于smoke test。所有回答必须基于本地资料，不确定时需明确说资料不足无法确定，不编造不存在功能。

## 训练标签
v0.1,megatrain,m1,token-dense
## 补充内容

### 补充1

MemoryQwen v0.1补充资料：本地AI agent系统。CLI操作入口。支持txt和md文件导入。不支持PDF/DOCX。BM25检索无向量搜索。Guardian检测。任务管理。无WebUI。7B模型推荐。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。

### 补充2

MemoryQwen v0.1补充资料：本地AI agent系统。CLI操作入口。支持txt和md文件导入。不支持PDF/DOCX。BM25检索无向量搜索。Guardian检测。任务管理。无WebUI。7B模型推荐。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。

### 补充3

MemoryQwen v0.1补充资料：本地AI agent系统。CLI操作入口。支持txt和md文件导入。不支持PDF/DOCX。BM25检索无向量搜索。Guardian检测。任务管理。无WebUI。7B模型推荐。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。

### 补充4

MemoryQwen v0.1补充资料：本地AI agent系统。CLI操作入口。支持txt和md文件导入。不支持PDF/DOCX。BM25检索无向量搜索。Guardian检测。任务管理。无WebUI。7B模型推荐。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。

### 补充5

MemoryQwen v0.1补充资料：本地AI agent系统。CLI操作入口。支持txt和md文件导入。不支持PDF/DOCX。BM25检索无向量搜索。Guardian检测。任务管理。无WebUI。7B模型推荐。回答基于本地资料和source引用。不确定时说无法确定。不编造功能。

