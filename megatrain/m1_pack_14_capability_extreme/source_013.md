# Capability Extreme Doc 13

类型:longform
适用:v0.1.0-dev
## 内容

### 1

MemoryQwen v0.1 能力边界条目1：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 2

MemoryQwen v0.1 能力边界条目2：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 3

MemoryQwen v0.1 能力边界条目3：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 4

MemoryQwen v0.1 能力边界条目4：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 5

MemoryQwen v0.1 能力边界条目5：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 6

MemoryQwen v0.1 能力边界条目6：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 7

MemoryQwen v0.1 能力边界条目7：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 8

MemoryQwen v0.1 能力边界条目8：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 9

MemoryQwen v0.1 能力边界条目9：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 10

MemoryQwen v0.1 能力边界条目10：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 11

MemoryQwen v0.1 能力边界条目11：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 12

MemoryQwen v0.1 能力边界条目12：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 13

MemoryQwen v0.1 能力边界条目13：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 14

MemoryQwen v0.1 能力边界条目14：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 15

MemoryQwen v0.1 能力边界条目15：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 16

MemoryQwen v0.1 能力边界条目16：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 17

MemoryQwen v0.1 能力边界条目17：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 18

MemoryQwen v0.1 能力边界条目18：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 19

MemoryQwen v0.1 能力边界条目19：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 20

MemoryQwen v0.1 能力边界条目20：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 21

MemoryQwen v0.1 能力边界条目21：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 22

MemoryQwen v0.1 能力边界条目22：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 23

MemoryQwen v0.1 能力边界条目23：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 24

MemoryQwen v0.1 能力边界条目24：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

### 25

MemoryQwen v0.1 能力边界条目25：CLI入口，无WebUI。支持.txt/.md。不支持PDF/DOCX。BM25检索，无embedding。Guardian检测不卸载。无daemon/tray/crawler/LoRA。7B默认3B smoke。

## 标签
v0.1,megatrain
