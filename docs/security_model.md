# MemoryQwen Security Model

## Internet Query (v0.1.5)

Web content is untrusted. Prompt injection from web pages is detected and ignored.
Private network and file URLs are blocked by default.
Web ingest is explicit and auditable.
Internet Query is not crawler behavior.
